{
  "compilerOptions": {
    // 采用的模块系统
    "module": "commonjs",
    // 编译输出目标 ES 版本
    "target": "es6",
    // 删除所有注释，除了以 /!*开头的版权信息。
    "removeComments": true
  },
  "exclude": [
    "node_modules"
  ]
}
